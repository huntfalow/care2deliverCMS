import { GeneratedSection } from "../models/generatedSection";
export class PageLayout {
  GeneratedSections : Array<GeneratedSection>;
  ApiUrl : string;
  Title : string;
  Identity:String; // identity herkent door de server
}